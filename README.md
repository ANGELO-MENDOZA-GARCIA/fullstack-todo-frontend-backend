# To-Do Fullstack (React + Node + SQLite)

Proyecto de ejemplo: frontend (Vite + React) y backend (Node + Express) con persistencia en SQLite.

## Requisitos
- Node.js (±) v18+ (o la versión que uses)
- npm
- Git

## Instalar y correr localmente

### 1. Clonar repo
```bash
git clone https://github.com/tuusuario/nombre-repo.git
cd nombre-repo
